NEXT_PUBLIC_ALIEN_SSO_BASE_URL=https://sso.alien-api.com
NEXT_PUBLIC_ALIEN_PROVIDER_ADDRESS=0000000604000000000036159b3c0f15
# ^ Still used by the frontend sign-in flow to identify this app's
# OAuth client to the SSO. NOT used by the agent-id verifier — since
# @alien-id/sso-agent-id 2.1.0 the verifier defaults to the federated
# audience pattern (any agent on the Alien SSO accepted).
DATABASE_URL=postgresql://user:password@localhost:5432/alienbook
